.playwright-mcp/
.playwright/
debug.log
node_modules/
playwright-report/
test-results/
.wrangler/
img-src/
.firebaserc

# graft: lokal grafcache, regenererbar, committas inte (kor `graft build`).
graft/

# repowise: lokalt index, regenererbart, committas inte (kor `repowise update`).
.repowise/

# AGENTS.md innehaller bara repowise autogenererade distill-avsnitt har, inga
# egna instruktioner, och halls darfor lokalt. Skriver du riktiga instruktioner:
# ta bort raden nedan forst, annars hamnar de aldrig i git.
AGENTS.md

# F3: kuratorns lokala förslagskopior (scripts/suggestions.js pull)
suggestions/
# lokala Worker-hemligheter för wrangler dev
worker/.dev.vars
